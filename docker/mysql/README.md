# MySQL compile (obsolete)

La stack utilise maintenant l'image prete **`vsamov/mysql-5.1.73`** (voir `docker-compose.yml`).

Les fichiers `Dockerfile` / `compile_mysql51.sh` de ce dossier ne sont plus utilises (telechargement MySQL 5.1.53 indisponible sur les mirrors).
